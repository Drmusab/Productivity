# Productivity OS - Docker Compose
# This file is a convenience wrapper that points to the main compose configuration
#
# Usage:
#   Development: docker compose --profile dev up
#   Production:  docker compose --profile prod up -d
#   Plane:       docker compose --profile plane up
#
# For more options, see: infra/docker/docker-compose.yml

include:
  - path: ./infra/docker/docker-compose.yml
  - path: ./infra/docker/docker-compose.plane.yml
